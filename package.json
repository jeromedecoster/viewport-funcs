{
  "name": "viewport-funcs",
  "version": "0.2.0",
  "description": "A very limited subset of viewport functions I use every day",
  "repository": "jeromedecoster/viewport-funcs",
  "main": "index.js",
  "files": [
    "cache.js",
    "contains.js",
    "index.js",
    "margins.js"
  ],
  "keywords": [
    "browser",
    "viewport",
    "document",
    "size",
    "position",
    "contains",
    "margins",
    "rect"
  ],
  "author": {
    "name": "Jérôme Decoster",
    "url": "http://jeromedecoster.com"
  },
  "license": "MIT",
  "scripts": {
    "start": "budo src/index.js:bundle.js --dir src --host localhost --port 3000 --no-debug --open",
    "dev": "budo src/index.js:bundle.js --dir src --host localhost --port 3000 --no-debug --live",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "budo": "^8.3.0"
  },
  "dependencies": {
    "is-funcs": "^0.2.1"
  }
}
